# ==================================================
# Functions
# ==================================================

mkcd() {
  mkdir -p "$1" && cd "$1"
}

take() {
  mkdir -p "$1" && cd "$1"
}

pathlist() {
  echo "$PATH" | tr ':' '\n'
}
